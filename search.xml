<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux简单命令</title>
    <url>/Linux/Linux%E7%AE%80%E5%8D%95%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h1>常用Linux命令的基本使用</h1>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文</th>
<th style="text-align:left">作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>ls</td>
<td>list</td>
<td style="text-align:left">查看当前文件夹下的内容</td>
</tr>
<tr>
<td>pwd</td>
<td>print work directory</td>
<td style="text-align:left">查看当前所在文件夹</td>
</tr>
<tr>
<td>cd[目录名]</td>
<td>change directory</td>
<td style="text-align:left">切换文件夹</td>
</tr>
<tr>
<td>touch[文件名]</td>
<td>touch</td>
<td style="text-align:left">如果文件不存在，新建文件</td>
</tr>
<tr>
<td>mkdir[目录名]</td>
<td>make directory</td>
<td style="text-align:left">创建目录</td>
</tr>
<tr>
<td>rm[文件名]</td>
<td>remove</td>
<td style="text-align:left">删除指定的文件名</td>
</tr>
<tr>
<td>clear</td>
<td>clear</td>
<td style="text-align:left">清屏</td>
</tr>
</tbody>
</table>
<blockquote>
<p>小技巧</p>
<ul>
<li><code>ctrl + shift + =</code>放大终端窗口的字体显示</li>
<li><code>ctrl + -</code>缩小终端窗口的字体显示</li>
</ul>
</blockquote>
<h2 id="Linux终端命令格式"><a class="header-anchor" href="#Linux终端命令格式">¶</a>Linux终端命令格式</h2>
<h3 id="终端命令格式"><a class="header-anchor" href="#终端命令格式">¶</a>终端命令格式</h3>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">command [-options] [parameter]</span><br></pre></td></tr></table></figure>
<p>解释:</p>
<ul>
<li><code>command</code> :命令名，相应功能的英文单词火单词的缩写</li>
<li><code>[-options]</code>:选项，可用来对命令进行控制，也可以省略</li>
<li><code>parameter</code>：传给命令的参数，可以是<strong>零个</strong>，<strong>一个</strong>或者<strong>多个</strong></li>
</ul>
<p><code>[]</code>代表可选</p>
<h3 id="查阅帮助信息"><a class="header-anchor" href="#查阅帮助信息">¶</a>查阅帮助信息</h3>
<h4 id="–help"><a class="header-anchor" href="#–help">¶</a>–help</h4>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">command --help</span><br></pre></td></tr></table></figure>
<p>说明：</p>
<ul>
<li>提示<code>command</code>命令中的帮助信息</li>
</ul>
<h3 id="man"><a class="header-anchor" href="#man">¶</a>man</h3>
<p><code>man command</code></p>
<p>说明：</p>
<ul>
<li>查阅<code>command</code>命令的使用手册</li>
</ul>
<blockquote>
<p><code>man</code>是<code>manual</code>的缩写，是<code>Linux</code>的一个<strong>手册</strong>，包含了绝大部分的命令，函数的详细说明</p>
</blockquote>
<p>使用<code>man</code>时的操作间</p>
<table>
<thead>
<tr>
<th><strong>操作键盘</strong></th>
<th><strong>功能</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>空格键</td>
<td>显示手册的下一屏</td>
</tr>
<tr>
<td>Enter键</td>
<td>一次滚动手册页的一行</td>
</tr>
<tr>
<td><code>b</code></td>
<td>回滚一屏</td>
</tr>
<tr>
<td><code>f</code></td>
<td>前滚一屏</td>
</tr>
<tr>
<td><code>q</code></td>
<td>退出</td>
</tr>
</tbody>
</table>
<h2 id="文件和目录常用命令"><a class="header-anchor" href="#文件和目录常用命令">¶</a>文件和目录常用命令</h2>
<ul>
<li>查看内容
<ul>
<li><code>ls</code></li>
</ul>
</li>
<li>切换目录
<ul>
<li><code>cd</code></li>
</ul>
</li>
<li>创建和删除操作
<ul>
<li><code>touch</code></li>
<li><code>rm</code></li>
<li><code>mkdir</code></li>
</ul>
</li>
<li>拷贝和移动文件
<ul>
<li><code>cp</code></li>
<li><code>mv</code></li>
</ul>
</li>
<li>查看文件内容
<ul>
<li><code>cat</code></li>
<li><code>more</code></li>
<li><code>grep</code></li>
</ul>
</li>
<li>其他
<ul>
<li><code>echo</code></li>
<li>重定向<code>&gt;</code>和<code>&gt;&gt;</code></li>
<li>管道<code>|</code></li>
</ul>
</li>
</ul>
<h2 id="查看目录内容"><a class="header-anchor" href="#查看目录内容">¶</a>查看目录内容</h2>
<h3 id="终端使用技巧"><a class="header-anchor" href="#终端使用技巧">¶</a>终端使用技巧</h3>
<h4 id="自动补全"><a class="header-anchor" href="#自动补全">¶</a>自动补全</h4>
<ul>
<li>在敲出<code>文件</code>/<code>目录</code>/<code>命令</code>的前几个字母之后，按下<code>tab</code>键盘
<ul>
<li>如果输入的没有歧义，系统会自动补全</li>
<li>如果还存在其他<code>文件</code>/<code>目录</code>/<code>命令</code>，在按一下<code>tab</code>键，系统会提示可能存在的命令</li>
</ul>
</li>
</ul>
<h4 id="曾经使用过的命令"><a class="header-anchor" href="#曾经使用过的命令">¶</a>曾经使用过的命令</h4>
<ul>
<li>按<code>上</code>/<code>下</code>光标可以在曾经使用过的命令之间来回切换</li>
<li>如果想要退出选择，并且不想执行当前选中的命令，可以按<code>ctrl + c</code></li>
</ul>
<h2 id="ls命令说明"><a class="header-anchor" href="#ls命令说明">¶</a>ls命令说明</h2>
<ul>
<li><code>ls</code>是英文单词<code>list</code>的，其功能为列出目录的内容，使用户最常用的命令之一，类似于DOS下的<code>dir</code>命令</li>
</ul>
<h2 id="Linux下文件和目录的特点"><a class="header-anchor" href="#Linux下文件和目录的特点">¶</a>Linux下文件和目录的特点</h2>
<ul>
<li>Linux<strong>文件</strong>或者<strong>目录</strong>名称最长可以有<code>256</code>个字符</li>
<li>以<code>.</code>开头的文件为隐藏文件，需要用<code>-a</code>参数才能显示</li>
<li><code>.</code>代表当前目录</li>
<li><code>..</code>代表上级目录</li>
</ul>
<h2 id="ls-常用选项"><a class="header-anchor" href="#ls-常用选项">¶</a>ls 常用选项</h2>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-a</td>
<td>显示指定目录下所有子目录与文件，包含隐藏文件</td>
</tr>
<tr>
<td>-l</td>
<td>以列表方式显示文件的详细信息</td>
</tr>
<tr>
<td>-h</td>
<td>配合<code>-l</code>以人性化的方式显示文件的大小</td>
</tr>
</tbody>
</table>
<h2 id="ls通配符的使用"><a class="header-anchor" href="#ls通配符的使用">¶</a>ls通配符的使用</h2>
<table>
<thead>
<tr>
<th>通配符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>*</td>
<td>代表任意个数个字符</td>
</tr>
<tr>
<td>？</td>
<td>代表任意一个字符</td>
</tr>
<tr>
<td>[]</td>
<td>表示可以匹配字符组中的任一一个</td>
</tr>
<tr>
<td>[abc]</td>
<td>匹配a,b,c中任意一个</td>
</tr>
<tr>
<td>[a-f]</td>
<td>匹配a-f中左右的字符</td>
</tr>
</tbody>
</table>
<h2 id="切换目录"><a class="header-anchor" href="#切换目录">¶</a>切换目录</h2>
<h3 id="cd"><a class="header-anchor" href="#cd">¶</a>cd</h3>
<ul>
<li>
<p><code>cd</code>是英文单词<strong>change directory</strong>的简写，其功能为更改当前的工作目录，也是用户最常用的命令之一</p>
<blockquote>
<p>注意：Linux所有的<strong>目录</strong>和<strong>文件名</strong>都是大小写敏感的</p>
</blockquote>
<table>
<thead>
<tr>
<th>命令</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>cd</td>
<td>切换到当前用户的主目录(/home/用户目录)</td>
</tr>
<tr>
<td>cd ~</td>
<td>切换到当前用户的主目录(/home/用户目录)</td>
</tr>
<tr>
<td>cd .</td>
<td>保持到当前目录不变</td>
</tr>
<tr>
<td>cd …</td>
<td>切换到上级目录</td>
</tr>
<tr>
<td>cd -</td>
<td>可以再最近两次工作目录来回切换</td>
</tr>
</tbody>
</table>
</li>
</ul>
<h3 id="相对路径和绝对路径"><a class="header-anchor" href="#相对路径和绝对路径">¶</a>相对路径和绝对路径</h3>
<ul>
<li><strong>相对路径</strong>在输入路径是，最前面不是/或者~，表示当前相对<strong>当前目录</strong>所在的目录位置</li>
<li><strong>绝对路径</strong>在输入路径是，最前面是/或者~，表示从<strong>根目录/家目录</strong>开始的具体目录位置</li>
</ul>
<h2 id="创建和删除操作"><a class="header-anchor" href="#创建和删除操作">¶</a>创建和删除操作</h2>
<h4 id="touch"><a class="header-anchor" href="#touch">¶</a>touch</h4>
<ul>
<li>创建文件或者修改文件时
<ul>
<li>如果文件<strong>不存在</strong>，可以创建一个空白文件</li>
<li>如果文件<strong>已经存在</strong>，可以修改文件的末次修改日期</li>
</ul>
</li>
</ul>
<h4 id="mkdir"><a class="header-anchor" href="#mkdir">¶</a>mkdir</h4>
<ul>
<li>
<p>创建一个新的目录</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-p</td>
<td>可以递归创建目录</td>
</tr>
</tbody>
</table>
<blockquote>
<p><strong>新建目录的名称</strong>不能与当前目录中<strong>已有的文件或者目录</strong>同名</p>
</blockquote>
</li>
</ul>
<h4 id="rm"><a class="header-anchor" href="#rm">¶</a>rm</h4>
<ul>
<li>删除文件或者目录</li>
</ul>
<blockquote>
<p>使用<code>rm</code>命令要小心，因为文件删除后不能恢复</p>
</blockquote>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>- f</td>
<td>强制删除，忽略不存在的文件，无需提示</td>
</tr>
<tr>
<td>-r</td>
<td>递归的删除目录下的内容，<strong>删除文件夹</strong>时必须加此参数</td>
</tr>
</tbody>
</table>
<h2 id="拷贝和移动文件"><a class="header-anchor" href="#拷贝和移动文件">¶</a>拷贝和移动文件</h2>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文名</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>tree[目录名]</td>
<td>tree</td>
<td>以树状图列出文件目录结构</td>
</tr>
<tr>
<td>cp 源文件</td>
<td>copy</td>
<td>复制文件或者目录</td>
</tr>
<tr>
<td>mv 源文件</td>
<td>move</td>
<td>移动文件或者目录/文件或者目录重命名</td>
</tr>
</tbody>
</table>
<h4 id="tree"><a class="header-anchor" href="#tree">¶</a>tree</h4>
<ul>
<li><code>tree</code>命令可以以树状图列出文件目录结构</li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-d</td>
<td>只显示目录</td>
</tr>
</tbody>
</table>
<h4 id="cp"><a class="header-anchor" href="#cp">¶</a>cp</h4>
<ul>
<li><code>cp</code>命令的功能是将给出的<strong>文件</strong>或<strong>目录</strong>复制到另外一个<strong>文件</strong>或<strong>目录</strong>中，相当于<strong>DOS</strong>下的<code>copy</code>命令</li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-f</td>
<td>已经存在的目标文件直接覆盖，不会提示</td>
</tr>
<tr>
<td>-i</td>
<td>覆盖文件前提示</td>
</tr>
<tr>
<td>-r</td>
<td>若给出的源文件是目录文件，则cp讲递归复制该目录下的所有子目录和文件，目标文件必须为一个文件名</td>
</tr>
</tbody>
</table>
<h4 id="mv"><a class="header-anchor" href="#mv">¶</a>mv</h4>
<ul>
<li>
<p><code>mv</code>命令可以用来<strong>移动文件</strong>或<strong>目录</strong>复制到另一个<strong>文件</strong>或<strong>目录</strong>，相当于<strong>DOS</strong>下的<code>copy</code>命令</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-i</td>
<td>覆盖文件前提示</td>
</tr>
</tbody>
</table>
</li>
</ul>
<h3 id="查看文件目录"><a class="header-anchor" href="#查看文件目录">¶</a>查看文件目录</h3>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>cat  文件名</td>
<td>concatenate</td>
<td>查看文件内容，创建文件，文件合并，追加文件内容等功能</td>
</tr>
<tr>
<td>more 文件名</td>
<td>more</td>
<td>分屏显示文件内容</td>
</tr>
<tr>
<td>grep 搜索文本文件名</td>
<td>grep</td>
<td>搜索文本文件目录</td>
</tr>
</tbody>
</table>
<h4 id="cat"><a class="header-anchor" href="#cat">¶</a>cat</h4>
<ul>
<li><code>cat</code>命令可用来<strong>查看文件内容</strong>，<strong>创建文件</strong>，<strong>文件合并</strong>，<strong>追加文件内容</strong>等功能</li>
<li><code>cat</code>会一次显示所有的内容，适合<strong>查看内容较少</strong>的文本文件</li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-b</td>
<td>对非空输出行编号</td>
</tr>
<tr>
<td>-n</td>
<td>对输出的所有行编号</td>
</tr>
</tbody>
</table>
<blockquote>
<p>Linux中还有一个<code>nl</code>的命令和<code>cat -b</code>的效果等价</p>
</blockquote>
<h4 id="more"><a class="header-anchor" href="#more">¶</a>more</h4>
<ul>
<li><code>more</code>命令可以用与分屏显示文件内容，每次只显示一夜的内容</li>
<li>适合<strong>查看内容较多</strong>的文本文件</li>
</ul>
<p>使用<code>more</code>的操作键:</p>
<table>
<thead>
<tr>
<th>操作键</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>空格键</td>
<td>显示手册的下一屏</td>
</tr>
<tr>
<td>Enter键</td>
<td>一次滚动手册页的一行</td>
</tr>
<tr>
<td>b</td>
<td>回滚一屏</td>
</tr>
<tr>
<td>f</td>
<td>前滚一屏</td>
</tr>
<tr>
<td>q</td>
<td>退出</td>
</tr>
<tr>
<td>/word</td>
<td>搜索word字符串</td>
</tr>
</tbody>
</table>
<h4 id="grep"><a class="header-anchor" href="#grep">¶</a>grep</h4>
<ul>
<li>Linux系统中<code>grep</code>命令是一种强大的文本搜索工具</li>
<li><code>grep</code>允许对文本文件进行<strong>模式</strong>查找，所谓模式查找，又被称为<strong>正则表达式</strong></li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-n</td>
<td>显示匹配行及行号</td>
</tr>
<tr>
<td>-v</td>
<td>显示不包含匹配文本的所有行(相当于求反)</td>
</tr>
<tr>
<td>-i</td>
<td>忽略大小写</td>
</tr>
</tbody>
</table>
<ul>
<li>常用的两种模式查找</li>
</ul>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>^a</td>
<td>行首，搜寻以<strong>a</strong>开头的行</td>
</tr>
<tr>
<td>ke$</td>
<td>行尾，搜寻以<strong>ke</strong>结束的行</td>
</tr>
</tbody>
</table>
<h3 id="其他"><a class="header-anchor" href="#其他">¶</a>其他</h3>
<h4 id="echo文字内容"><a class="header-anchor" href="#echo文字内容">¶</a>echo文字内容</h4>
<ul>
<li><code>echo</code>会在终端中显示参数指定的文字，通常回合<strong>重定向</strong>联合使用</li>
</ul>
<h4 id="重定向-和"><a class="header-anchor" href="#重定向-和">¶</a>重定向&gt;和&gt;&gt;</h4>
<ul>
<li>Linux允许将命令指向结果重定向到一个文件</li>
<li>将本应显示在<strong>终端上得内容输出/追加</strong>到<strong>指定文件</strong>中</li>
</ul>
<p>其中</p>
<ul>
<li><code>&gt;</code>表示输出，会覆盖原有得内容</li>
<li><code>&gt;&gt;</code>表示追加，会将内容追加到已有文件的末尾</li>
</ul>
<h4 id="管道"><a class="header-anchor" href="#管道">¶</a>管道|</h4>
<ul>
<li>Linux允许将<strong>一个命令得输出</strong>可以<strong>通过管道</strong>做为<strong>另一个命令的输入</strong></li>
<li>可以理解显示生活中的管子，管子的一头塞东西进去，另外一头取出来，这里的<code>|</code>分为左右两端，左端塞东西(写)，右端取东西(读)</li>
</ul>
<p>常用的管道命令有：</p>
<ul>
<li><code>more</code>：分屏显示内容</li>
<li><code>grep</code>：在命令指向结果的基础上查找指定的文件</li>
</ul>
<h1>远程管理常用命令</h1>
<h2 id="目标"><a class="header-anchor" href="#目标">¶</a>目标</h2>
<ul>
<li>关机/重启
<ul>
<li><code>shutdown</code></li>
</ul>
</li>
<li>查看或配置网卡信息
<ul>
<li><code>ifconfig</code></li>
<li><code>ping</code></li>
</ul>
</li>
<li>远程登录和复制文件
<ul>
<li><code>ssh</code></li>
<li><code>scp</code></li>
</ul>
</li>
</ul>
<h2 id="关机-重启"><a class="header-anchor" href="#关机-重启">¶</a>关机/重启</h2>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>shutdown 选项  时间</td>
<td>shutdown</td>
<td>关机/重启</td>
</tr>
</tbody>
</table>
<h3 id="shutdown"><a class="header-anchor" href="#shutdown">¶</a>shutdown</h3>
<ul>
<li><code>shutdown</code>命令可以<strong>安全关闭</strong>或者<strong>重新启动系统</strong></li>
</ul>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-r</td>
<td>重新启动</td>
</tr>
</tbody>
</table>
<blockquote>
<p>提示：</p>
<ul>
<li><strong>不指定选项和参数</strong>，默认表示<strong>1分钟</strong>之后<strong>关闭电脑</strong></li>
<li>远程维护服务器是，最好不要关闭系统</li>
</ul>
</blockquote>
<ul>
<li>常用命令示例</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 重新启动操作系统，其中now表示现在</span><br><span class="line">$ shutdown -r now</span><br><span class="line"></span><br><span class="line"># 立刻关机，其中 now 表示现在</span><br><span class="line">$ shutdown now</span><br><span class="line"></span><br><span class="line"># 系统在今天的 20:25 会关机</span><br><span class="line">$ shutdown 20:25</span><br><span class="line"></span><br><span class="line"># 系统再过十分钟后自动关机</span><br><span class="line">$ shutdown +10</span><br><span class="line"></span><br><span class="line"># 取消之前指定的关机计划</span><br><span class="line">$ shutdown -c</span><br></pre></td></tr></table></figure>
<h2 id="查看或配置网卡信息"><a class="header-anchor" href="#查看或配置网卡信息">¶</a>查看或配置网卡信息</h2>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文</th>
</tr>
</thead>
<tbody>
<tr>
<td>ifconfig</td>
<td>configure  a network interface</td>
</tr>
<tr>
<td>ping ip地址</td>
<td>ping</td>
</tr>
</tbody>
</table>
<h3 id="网卡和IP地址"><a class="header-anchor" href="#网卡和IP地址">¶</a>网卡和IP地址</h3>
<h4 id="网卡"><a class="header-anchor" href="#网卡">¶</a>网卡</h4>
<ul>
<li>网卡是一个专门负责网络通讯的硬件设备</li>
<li><strong>IP地址</strong>是设置在网卡上的地址信息</li>
</ul>
<blockquote>
<p><em>我们可以把<strong>电脑</strong>比作<strong>电话</strong>，<strong>网卡</strong>相当于<strong>SIM卡，IP地址</strong>相当于<strong>电话号码</strong></em></p>
</blockquote>
<h4 id="IP地址"><a class="header-anchor" href="#IP地址">¶</a>IP地址</h4>
<ul>
<li><strong>每台联网的电脑上</strong>都有<strong>IP地址，是保证电脑之间正常通讯的重要设置</strong></li>
</ul>
<blockquote>
<p>注意：每天电脑的IP地址不能相同，否则会出现IP地址冲突，并且无法正常通讯</p>
<p>提示：有关IP地址的详细内容自己百度</p>
</blockquote>
<h3 id="ifconfig"><a class="header-anchor" href="#ifconfig">¶</a>ifconfig</h3>
<ul>
<li><code>ifconfig</code>可以查看/配置计算机当前的网卡配置信息</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 查看网卡配置信息</span><br><span class="line">$ ifconfig</span><br><span class="line"></span><br><span class="line"># 查看王阿库对应的 IP 地址</span><br><span class="line">$ ifconfig | grep inet</span><br></pre></td></tr></table></figure>
<blockquote>
<p>提示:一台计算机中有可能会有一个<strong>物理网卡</strong>和<strong>多个虚拟网卡</strong>，在Linux中的物理网卡的名字通常以<code>ensXX</code>表示</p>
</blockquote>
<ul>
<li><code>127.0.0.1</code>被称为<strong>本地回环/环回地址</strong>，一般用来测试本机网卡是否正常</li>
</ul>
<h3 id="ping"><a class="header-anchor" href="#ping">¶</a>ping</h3>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 检测到目标主机正常连接</span><br><span class="line">$ ping IP地址</span><br><span class="line"></span><br><span class="line"># 检测本地网卡工作正常</span><br><span class="line">$ ping 127.0.0.1</span><br></pre></td></tr></table></figure>
<ul>
<li><code>ping</code>一般用于检测当前计算几到目标计算机之间的网络<strong>是否通畅，数值越大，速度越慢</strong></li>
</ul>
<blockquote>
<ul>
<li><code>ping</code>的工作原理与潜水艇的声纳像是，<code>ping</code>这个命令就是取自<strong>声纳的声音</strong></li>
<li>网络管理员之间也常将<code>ping</code>用作东侧-----<strong>ping一下计算机X，看他是否开着</strong></li>
</ul>
</blockquote>
<p>原理：网络上的机器又有 <strong>唯一确定的IP地址</strong>，我们给<strong>目标IP地址</strong>发送一个数据包，对方就要返回一个数据包，根据返回的数据包以及时间，我们就可以确定目标主机的存在</p>
<blockquote>
<p>提示：在Linux中，想要终止一个终端程序的执行，绝大多数都可以使用<code>CTRL+C</code></p>
</blockquote>
<h2 id="远程登录和复制文件"><a class="header-anchor" href="#远程登录和复制文件">¶</a>远程登录和复制文件</h2>
<table>
<thead>
<tr>
<th>命令</th>
<th>对应英文</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>ssh 用户名@ip</td>
<td>secure shell</td>
<td>关机/重新启动操作系统</td>
</tr>
<tr>
<td>scp 用户名@ip:文件名或路径 用户名@ip：文件名或路径</td>
<td>secure copy</td>
<td>远程复制文件</td>
</tr>
</tbody>
</table>
<h3 id="SSH基础-重点"><a class="header-anchor" href="#SSH基础-重点">¶</a>SSH基础(重点)</h3>
<p>在Linux中SSH是<strong>非常常用</strong>的工具，通过<strong>SSH 客户端</strong>我们可以连接到运行了<strong>SSH服务器</strong>的远程机器上</p>
<p><img src="/images/Linux%E7%AE%80%E5%8D%95%E5%91%BD%E4%BB%A4/1.png" alt="1"></p>
<ul>
<li><strong>SSH客户端</strong>是一种使用<code>Secure Shell(SSH)</code>协议连接到远程计算机的软件程序</li>
<li><code>SSH</code>是目前较可靠，<strong>专为远程登录回话和其他网络服务</strong>提供安全性的协议
<ul>
<li>利用<code>SSH 协议</code>可以有效<strong>防止远程管理过程中的信息泄露</strong></li>
<li>通过<code>SSH协议</code>可以对所有传输的数据进行加密，也能够防止DNS欺骗和IP欺骗</li>
</ul>
</li>
<li><code>SSH</code>的另一项有点是对传输的数据可以是经过压缩的，所以可以加快传输的速度</li>
</ul>
<h4 id="域名-和端口"><a class="header-anchor" href="#域名-和端口">¶</a>域名 和端口</h4>
<h5 id="域名"><a class="header-anchor" href="#域名">¶</a>域名</h5>
<ul>
<li>由一串<strong>用点分割</strong>的名字组成，例如：<code>www.baidu.com</code></li>
<li>是<strong>IP地址</strong>的别名，方便用户记忆</li>
</ul>
<h5 id="端口号"><a class="header-anchor" href="#端口号">¶</a>端口号</h5>
<ul>
<li><strong>IP地址</strong>：通过<strong>IP地址</strong>扎到网络上的计算机</li>
<li><strong>端口号</strong>：通过<strong>端口号</strong>可以找到<strong>计算机上运行的应用程序</strong>
<ul>
<li><strong>SSH服务器</strong>的默认端口号是<code>22</code>，如果是默认端口号，在连接的时候，可以省略</li>
</ul>
</li>
<li>常见服务端口号列表：</li>
</ul>
<table>
<thead>
<tr>
<th>服务</th>
<th>端口号</th>
</tr>
</thead>
<tbody>
<tr>
<td>SSH服务器</td>
<td>22</td>
</tr>
<tr>
<td>Web服务器</td>
<td>80</td>
</tr>
<tr>
<td>HTTPS</td>
<td>443</td>
</tr>
<tr>
<td>FTP服务器</td>
<td>21</td>
</tr>
</tbody>
</table>
<blockquote>
<p>提示：有关<strong>端口号</strong>的详细内容，请百度</p>
</blockquote>
<h4 id="SSH客户端的简单使用"><a class="header-anchor" href="#SSH客户端的简单使用">¶</a>SSH客户端的简单使用</h4>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh [-p port] user@remote</span><br></pre></td></tr></table></figure>
<ul>
<li><code>user</code>是在远程机器上的目录名，如果不指定的话默认为当前用户</li>
<li><code>remote</code>是远程机器的地址，可以是<strong>IP/域名</strong>，或者是<strong>后面会提到的别名</strong></li>
<li><code>port</code>是<strong>SSH Server监听的端口</strong>，如果不指定，就为默认值<code>22</code></li>
</ul>
<blockquote>
<p>提示：</p>
<ul>
<li>使用<code>exit</code>退出当前用户的登陆</li>
</ul>
<p>注意：</p>
<ul>
<li><code>ssh</code>这个终端命令只能在<code>Linux</code>或者<code>UNIX</code>系统下使用</li>
<li>如果在<code>Windows</code>系统中，可以安装<code>PuTTY</code>或者<code>XShell</code>客户端软件即可</li>
</ul>
<p>提示：</p>
<ul>
<li>在工作中，SSH服务器的端口号很有可能<strong>不是22</strong>，如果遇到这种情况就需要使用<code>-p</code>选项，指定正确的端口号，否则无法正常连接到服务器</li>
</ul>
</blockquote>
<h3 id="scp"><a class="header-anchor" href="#scp">¶</a>scp</h3>
<ul>
<li>scp就是<code>secure copy</code>，是一个在Linux下用来进行<strong>远程拷贝文件</strong>的命令</li>
<li>它的<strong>地址格式与ssh基本相同，需要注意的是</strong>，在指定端口时用的是大写的<code>-p</code>而不是小写的</li>
</ul>
<p><img src="/source/images/Linux%E7%AE%80%E5%8D%95%E5%91%BD%E4%BB%A4/image-20200424010226836.png" alt="image-20200424010226836"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 把本地目录下的 01.py 文件复制到远程家目录下的Desktop&#x2F;01.py</span><br><span class="line"># 注意; &#96;:&#96;后面的路径如果不是绝对路径，则以用户的家目录作为参考路径</span><br><span class="line">scp -P port 01.py user@remote:Desktop&#x2F;01.py</span><br><span class="line"></span><br><span class="line"># 把远程家目录下的Desktop&#x2F;01.py文件复制到本地当前目录下的01.py</span><br><span class="line">scp -P port user@remote:Desktop&#x2F;01.py 01.py</span><br><span class="line"></span><br><span class="line"># 加上 -r 选项可以传送文件夹</span><br><span class="line"># 把当前目录下的demo文件夹复制到远程家目录下的Desktop</span><br><span class="line">scp -r demo user@remote:Desktop</span><br><span class="line"></span><br><span class="line"># 把远程 家目录下的Desktop复制到当前目录下的demo文件夹</span><br><span class="line">scp -r user@remote:Desktop demo</span><br></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th>选项</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>-r</td>
<td>若给出的源文件是目录文件，则scp</td>
</tr>
<tr>
<td>-P</td>
<td>若远程SSH服务器的端口是22，需要使用大写字母-P选项指定端口</td>
</tr>
</tbody>
</table>
<h4 id="SSH高级"><a class="header-anchor" href="#SSH高级">¶</a>SSH高级</h4>
<ul>
<li>免密码登陆</li>
<li>配置别名</li>
</ul>
<blockquote>
<p>提示：有关SSH配置信息都保存在用户家目录下的<code>.ssh</code>目录下</p>
</blockquote>
<h5 id="免密登陆"><a class="header-anchor" href="#免密登陆">¶</a>免密登陆</h5>
<p>步骤</p>
<ul>
<li>配置公钥
<ul>
<li>执行<code>ssh-keygen</code>即可生成SSH钥钥，一路回车即可</li>
</ul>
</li>
<li>上传公钥到服务器
<ul>
<li>执行<code>ssh-copy-id -p port user@remote</code>，可以让远程服务器记住我们的公钥</li>
</ul>
</li>
</ul>
<p>示意图</p>
<p><img src="/source/images/Linux%E7%AE%80%E5%8D%95%E5%91%BD%E4%BB%A4/image-20200424005617262.png" alt="image-20200424005617262"></p>
<blockquote>
<p>非对称加密算法</p>
<ul>
<li>使用<strong>公钥</strong>加密的数据，需要<strong>私钥</strong>解密</li>
<li>使用<strong>私钥</strong>加密的数据，需要使用<strong>公钥</strong>解密</li>
</ul>
</blockquote>
<h5 id="配置别名"><a class="header-anchor" href="#配置别名">¶</a>配置别名</h5>
<p>每次都输入<code>ssh -p port user@remote</code>时间久了会觉得很麻烦，特别是当<code>user</code>,<code>remore</code>和<code>port</code>都输入，而且还不好记忆</p>
<p>而 <strong>配置别名</strong>可以让我们进一步偷懒，譬如用：<code>ssh mac</code>来替代上面这么长一串，那么就在<code>~/.ssh/config</code>里面追加以下内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Host mac</span><br><span class="line">	HostName ip地址</span><br><span class="line">	User xxx</span><br><span class="line">	port  22</span><br></pre></td></tr></table></figure>
<p><strong>保存之后，即可用<code>ssh mac</code>实现远程登陆了，<code>scp</code>同样可以使用</strong></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>简单命令</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux主要目录</title>
    <url>/Linux/Linux%E4%B8%BB%E8%A6%81%E7%9B%AE%E5%BD%95/</url>
    <content><![CDATA[<h2 id="Linux主要目录"><a class="header-anchor" href="#Linux主要目录">¶</a>Linux主要目录</h2>
<h3 id=""><a class="header-anchor" href="#">¶</a>/</h3>
<p>/:根目录</p>
<p>一般根目录只存放目录，在<code>Linux</code>下有且只有一个根目录，所有的东西都是从这里开始的</p>
<ul>
<li>当在终端里输入<code>/home</code>，其实是在告诉电脑，先从<code>/</code>(根目录)开始，在进入到<code>home</code>目录</li>
</ul>
<h3 id="bin-usr-bin"><a class="header-anchor" href="#bin-usr-bin">¶</a>/bin,/usr/bin</h3>
<p>可执行的二进制文件的目录，如常用的命令<code>ls</code>,<code>tar</code>,<code>mv</code>,<code>cat</code>等</p>
<h3 id="boot"><a class="header-anchor" href="#boot">¶</a>/boot</h3>
<p>放置<code>Linux</code>系统启动时用到的一些文件,如<code>Linux</code>的内核文件:<code>/boot/vmlinux</code>，系统引导器<code>/boot/grub</code></p>
<h3 id="dev"><a class="header-anchor" href="#dev">¶</a>/dev</h3>
<p>存放Linux系统下的设备文件，访问该目录下某个文件，相当于访问某个设备，常用的是挂载光驱<code>mount/dev/cdrom/mnt</code></p>
<h3 id="etc"><a class="header-anchor" href="#etc">¶</a>/etc</h3>
<p>系统配置文件的存放目录，不建议在此目录下存放可执行文件，重要的配置文件有</p>
<ul>
<li><code>/etc/inittab</code></li>
<li><code>/etc/fstab</code></li>
<li><code>/etc/init.d</code></li>
<li><code>/etc/X11</code></li>
<li><code>/etc/sysconfig</code></li>
<li><code>/etc/xinetd.d</code></li>
</ul>
<h3 id="home"><a class="header-anchor" href="#home">¶</a>/home</h3>
<p>系统默认的用户家目录，新增用户账号是，用户的家目录都存放在此目录下</p>
<ul>
<li><code>~</code>表示当前用户的家目录</li>
<li><code>~edu</code>表示用户<code>edu</code>的家目录</li>
</ul>
<h3 id="lib，-usr-lib，-usr-local-lib"><a class="header-anchor" href="#lib，-usr-lib，-usr-local-lib">¶</a>/lib，/usr/lib，/usr/local/lib</h3>
<p>系统使用的函数库的目录，程序在执行的过程中，需要调用一些额外的参数时需要函数库的协助</p>
<h3 id="lost-fount"><a class="header-anchor" href="#lost-fount">¶</a>/lost+fount</h3>
<p>系统异常产生错误时，会有一些遗失的片段放置于此目录下</p>
<h3 id="mnt-media"><a class="header-anchor" href="#mnt-media">¶</a>/mnt,/media</h3>
<p>光盘默认挂载点，通常光盘挂载与<code>/mnt/cdrom</code>下，也不一定，可以选择任意位置进行挂载</p>
<h3 id="opt"><a class="header-anchor" href="#opt">¶</a>/opt</h3>
<p>给主机额外安装软件所摆放的目录</p>
<h3 id="proc"><a class="header-anchor" href="#proc">¶</a>/proc</h3>
<p>此目录的数据都在内存中，如系统核心，外部设备，网络状态，由于数据都存放在内存中，所以不占用磁盘空间，比较重要的文件有:<code>/proc/cpuinfo</code>,<code>/proc/interrupts</code>,<code>proc/dma</code>,<code>proc/ioports</code>,``/proc/net/*</p>
<h3 id="root"><a class="header-anchor" href="#root">¶</a>/root</h3>
<p>系统管理员的root的家目录</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>入门学习</tag>
      </tags>
  </entry>
  <entry>
    <title>Markdown 学习</title>
    <url>/cmpr/markdown-%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1>基础</h1>
<h2 id="标题"><a class="header-anchor" href="#标题">¶</a>标题</h2>
<h3 id="1-使用-和-标记一级和二级标题"><a class="header-anchor" href="#1-使用-和-标记一级和二级标题">¶</a>1.使用<code>=</code>和<code>-</code>标记一级和二级标题</h3>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">一级标题</span><br><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">二级标题</span><br><span class="line">-------</span><br></pre></td></tr></table></figure>
<p>效果图如下
<img src="/images/markdown/1.png" alt=""></p>
<h3 id="2-使用-号标记"><a class="header-anchor" href="#2-使用-号标记">¶</a>2.使用<code>#</code>号标记</h3>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 一级标题</span><br><span class="line">## 二级标题</span><br><span class="line">### 三级标题</span><br><span class="line">#### 四级标题</span><br><span class="line">##### 五级标题</span><br><span class="line">###### 六级标题</span><br></pre></td></tr></table></figure>
<p>注：每个<code>#</code>号后最好都有一个空格</p>
<p>效果图如下
<img src="/images/markdown/2.png" alt=""></p>
<h2 id="段落"><a class="header-anchor" href="#段落">¶</a>段落</h2>
<h3 id="1-段落"><a class="header-anchor" href="#1-段落">¶</a>1.段落</h3>
<p>markdown段落没有特殊的格式，直接编写文字就好<strong>段落的换行是使用两个以上空格加回车</strong></p>
<h3 id="2-字体"><a class="header-anchor" href="#2-字体">¶</a>2.字体</h3>
<p>可以使用如下字体</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">* 斜体文本 *</span><br><span class="line">_ 斜体文本 _</span><br><span class="line">** 粗体文本 **</span><br><span class="line">__ 粗体文本 __</span><br><span class="line">*** 粗斜体文本 ***</span><br><span class="line">___ 粗斜体文本 ___</span><br></pre></td></tr></table></figure>
<p>注:上面文字前面和后面都有一个空格，把空格去掉才是正确使用方法
效果图如下
<em>斜体</em>
<em>斜体</em>
<strong>粗斜体</strong>
<strong>粗斜体</strong>
<em><strong>粗斜体</strong></em>
<em><strong>粗斜体</strong></em><br>
注:下划线都是英文的下划线</p>
<h3 id="3-分隔线"><a class="header-anchor" href="#3-分隔线">¶</a>3.分隔线</h3>
<p>可以使用星号，减号，底线来建立分割线，行内不能有其他东西。也可在型号或者减号中间出入空格.以下是写法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">***</span><br><span class="line">* * *</span><br><span class="line">*****</span><br><span class="line">- - -</span><br><span class="line">---------</span><br></pre></td></tr></table></figure>
<p>效果图</p>
<p><img src="/images/markdown/3.png" alt=""></p>
<h3 id="4-删除线"><a class="header-anchor" href="#4-删除线">¶</a>4.删除线</h3>
<p>如果要在文字上添加上出现，只需要在文字的两端加上波浪线就~~可以了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hape</span><br><span class="line">~~hape~~</span><br></pre></td></tr></table></figure>
<p>效果图
hape
<s>hape</s></p>
<h3 id="5-下划线"><a class="header-anchor" href="#5-下划线">¶</a>5.下划线</h3>
<p>下划线可以噶通过HTML的<code>&lt;u&gt;</code>标签来实现:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;u&gt;xxxx&lt;&#x2F;u&gt;</span><br></pre></td></tr></table></figure>
<p>效果:
<u>xxx</u></p>
<h3 id="6-脚注"><a class="header-anchor" href="#6-脚注">¶</a>6.脚注</h3>
<p>脚注是对文本的补充说明:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[^你想要标注的内容]</span><br></pre></td></tr></table></figure>
<p>效果图:<br>
python:<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p>
<h2 id="列表"><a class="header-anchor" href="#列表">¶</a>列表</h2>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">* 第一项</span><br><span class="line">* 第二项</span><br><span class="line">* 第三项  </span><br><span class="line">+ 第一项</span><br><span class="line">+ 第二项</span><br><span class="line">+ 第三项  </span><br><span class="line">- 第一项</span><br><span class="line">- 第二项</span><br><span class="line">- 第三项</span><br></pre></td></tr></table></figure>
<p>显示结果:</p>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
<h3 id="列表嵌套"><a class="header-anchor" href="#列表嵌套">¶</a>列表嵌套</h3>
<p>列表嵌套只需在子列表中的选项添加四个空格即可</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1.第一项:  </span><br><span class="line">  - 嵌套在第一项中的第一个元素  </span><br><span class="line">  - 嵌套在第一项中的第二个元素</span><br><span class="line">2.第二项:  </span><br><span class="line">  - 嵌套在第二项中的第一个元素  </span><br><span class="line">  - 嵌套在第二项中的第二个元素</span><br></pre></td></tr></table></figure>
<p>显示结果:<br>
1.第一项:</p>
<ul>
<li>嵌套在第一项中的第一个元素</li>
<li>嵌套在第一项中的第二个元素</li>
</ul>
<p>2.第二项:</p>
<ul>
<li>嵌套在第二项中的第一个元素</li>
<li>嵌套在第二项中的第二个元素</li>
</ul>
<h2 id="区块"><a class="header-anchor" href="#区块">¶</a>区块</h2>
<p>markdown区块引用时在段落开头使用<code>&gt;</code>符号，然后紧跟一个空格符号</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt; 区块应用</span><br></pre></td></tr></table></figure>
<p>区块嵌套</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt; 最外层</span><br><span class="line">&gt; &gt; 第一层嵌套</span><br><span class="line">&gt; &gt; &gt; 第二层嵌套</span><br></pre></td></tr></table></figure>
<p>效果图:</p>
<blockquote>
<p>最外层</p>
<blockquote>
<p>第一层嵌套</p>
<blockquote>
<p>第二层嵌套</p>
</blockquote>
</blockquote>
</blockquote>
<h3 id="区块中使用列表"><a class="header-anchor" href="#区块中使用列表">¶</a>区块中使用列表</h3>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt; 区块中使用列表</span><br><span class="line">&gt; 1.第一项</span><br><span class="line">&gt; 2.第二项</span><br><span class="line">&gt; + 第一项</span><br><span class="line">&gt; + 第二项</span><br><span class="line">&gt; + 第三项</span><br></pre></td></tr></table></figure>
<p>效果:</p>
<blockquote>
<p>区块中使用列表
1.第一项
2.第二项</p>
<ul>
<li>第一项</li>
<li>第二项</li>
<li>第三项</li>
</ul>
</blockquote>
<h3 id="列表中使用区块"><a class="header-anchor" href="#列表中使用区块">¶</a>列表中使用区块</h3>
<p>如果要在列表中放进去区块，需要在在<code>&gt;</code>前添加四个空格的缩进</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">* 第一项</span><br><span class="line">    &gt; 第一个区块</span><br><span class="line">    &gt; 第二个区块</span><br><span class="line">* 第二项</span><br></pre></td></tr></table></figure>
<p>效果:</p>
<ul>
<li>第一项
<blockquote>
<p>第一个区块
第二个区块</p>
</blockquote>
</li>
<li>第二项</li>
</ul>
<h2 id="代码"><a class="header-anchor" href="#代码">¶</a>代码</h2>
<p>如果你只是想把一个东西包起来,像这样<code>x</code>，那么只需要这样:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#96;x&#96;</span><br></pre></td></tr></table></figure>
<h3 id="代码区块"><a class="header-anchor" href="#代码区块">¶</a>代码区块</h3>
<p>使用<code>```</code>包裹一段代码，并指定一种语言(也可以不指定)</p>
<p>效果图：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">"Helloworld"</span>)</span><br></pre></td></tr></table></figure>
<h2 id="链接"><a class="header-anchor" href="#链接">¶</a>链接</h2>
<p>链接使用方法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[链接名称](链接地址)  </span><br><span class="line">或者  </span><br><span class="line">&lt;链接地址&gt;</span><br></pre></td></tr></table></figure>
<p>例如:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">这是一个链接[百度](www.baidu.com)</span><br></pre></td></tr></table></figure>
<p>效果:
这是一个链接<a href="www.baidu.com">百度</a></p>
<h2 id="图片"><a class="header-anchor" href="#图片">¶</a>图片</h2>
<p>用法:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">![](路径名)</span><br></pre></td></tr></table></figure>
<h2 id="表格"><a class="header-anchor" href="#表格">¶</a>表格</h2>
<p>markdown制作表格使用<code>|</code>来分割不同的单元格，使用<code>-</code>来分割表头和其他行.语法如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">| 表头 | 表头|</span><br><span class="line">| ---- | ---- |</span><br><span class="line">| 单元格 | 单元格 |</span><br><span class="line">| 单元格 | 单元格 |</span><br></pre></td></tr></table></figure>
<p>效果:</p>
<table>
<thead>
<tr>
<th>表头</th>
<th>表头</th>
</tr>
</thead>
<tbody>
<tr>
<td>单元格</td>
<td>单元格</td>
</tr>
<tr>
<td>单元格</td>
<td>单元格</td>
</tr>
</tbody>
</table>
<h3 id="对齐方式"><a class="header-anchor" href="#对齐方式">¶</a>对齐方式:</h3>
<p><code>-:</code>设置内容和标题栏居右对齐<br>
<code>:-</code>设置内容和标题栏居左对齐
<code>:-:</code>设置内容和标题栏居中对齐
实现如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">| 左对齐 | 右对齐 | 居中对齐|</span><br><span class="line">| ：-----| ----：|：----：|</span><br><span class="line">|单元格|单元格|单元格|</span><br><span class="line">|单元格|单元格|单元格|</span><br></pre></td></tr></table></figure>
<p>效果图</p>
<table>
<thead>
<tr>
<th style="text-align:right">右对齐</th>
<th style="text-align:left">左对齐</th>
<th style="text-align:center">居中对齐</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:right">单元格</td>
<td style="text-align:left">单元格</td>
<td style="text-align:center">单元格</td>
</tr>
<tr>
<td style="text-align:right">单元格</td>
<td style="text-align:left">单元格</td>
<td style="text-align:center">单元格</td>
</tr>
</tbody>
</table>
<h1>进阶</h1>
<h2 id="支持HTML元素"><a class="header-anchor" href="#支持HTML元素">¶</a>支持HTML元素</h2>
<p>很多HTML元素都能在markdown文件中使用</p>
<p>在更新中…</p>
<h3 id="做思维导图"><a class="header-anchor" href="#做思维导图">¶</a>做思维导图</h3>
<p>不想做了…</p>
<h3 id="公式编辑"><a class="header-anchor" href="#公式编辑">¶</a>公式编辑</h3>
<p>我的博客不允许我公式编辑,查了很多资料也还没有找到,如果你知道的话,请告诉我…</p>
<hr class="footnotes-sep">
<section class="footnotes">
<ol class="footnotes-list">
<li id="fn1" class="footnote-item"><p>一种编程语言 <a href="#fnref1" class="footnote-backref">↩︎</a></p>
</li>
</ol>
</section>
]]></content>
      <categories>
        <category>cmpr</category>
      </categories>
      <tags>
        <tag>markdown</tag>
      </tags>
  </entry>
</search>
